# Vibe Coding 项目开发流程 — AI 驱动开发完整指南

> 来源：鱼皮 AI 知识库 · 编程导航  
> 适用场景：从想法到商业级产品的 AI 辅助开发

---

## 为什么需要标准流程？

传统编程中，规划不清最多代码质量差；但在 Vibe Coding 中，规划不清 → AI "自由发挥" → 代码失控。

**核心原则**：AI 是强大的**执行者**，不是魔法。你需要清晰的规划让 AI 理解你的意图。

---

## 5 步工作流总览

```
需求研究 → PRD → 技术设计 → AI 代理指令 → 实现与迭代
(Research)  (PRD)  (Tech Design)  (AGENTS.md)    (Build)
```

---

## 步骤一：需求研究 (Research)

**文件**：`RESEARCH.md`

搞清楚：**做什么？为什么做？有没有人做过？**

### 操作指南

1. **明确目标** — 想解决什么问题？核心用户是谁？
2. **调研类似产品** — 看竞品优缺点，问 AI："有哪些类似产品？"
3. **记录发现** — 用非正式文档记录想法

### 示例模板

```markdown
# 项目需求研究

## 目标
[一句话描述项目目标]

## 调研发现
- 竞品 A：优点/缺点
- 竞品 B：优点/缺点
- 市场空白/切入点

## 核心需求（初步）
1. [功能 1]
2. [功能 2]
3. [功能 3]
```

---

## 步骤二：产品需求文档 (PRD)

**文件**：`PRD.md`

明确 **做什么** 和 **不做什么**，是整个流程中最重要的一步。

### 操作方法

1. 自己写一段简短需求描述
2. 让 AI 帮你扩展为结构化 PRD

> **推荐 Prompt**：
> ```
> 请帮我把这个需求扩展成完整的产品需求文档（PRD），
> 要包含：
> 1. 产品概述和目标用户
> 2. 详细的功能列表
> 3. 功能优先级（MVP 和后续版本）
> 4. 界面设计要求
> 5. 技术栈建议
> 6. 非功能性需求（性能、安全等）
> ```

### 示例模板

```markdown
# 项目 PRD

## 产品概述
[一句话介绍产品]

## 目标用户
[谁会用]

## 核心功能

### MVP（必须做）
1. [功能 A]
2. [功能 B]

### V2（后续做）
- [功能 C]
- [功能 D]

## 界面设计
[简要描述页面布局]

## 技术栈建议
- 前端：
- 后端：
- 数据库：

## 限制条件
- [边界场景]
- [约束条件]
```

---

## 步骤三：技术设计文档 (Tech Design)

**文件**：`TECH_DESIGN.md`

确定**用什么技术**和**大致架构**。

### 包含内容

- 技术栈选择及理由
- 项目结构 / 目录组织
- 数据模型设计
- 关键技术点 / 难点
- API 接口设计（如适用）

### 示例模板

```markdown
# 技术设计文档

## 技术栈
- 前端：React + TypeScript + Tailwind
- 状态管理：Zustand
- 数据存储：LocalStorage
- 构建工具：Vite

## 项目结构
```
src/
├── components/    # UI 组件
├── pages/         # 页面
├── hooks/         # 自定义 Hooks
├── utils/         # 工具函数
└── types/         # 类型定义
```

## 数据模型
```typescript
interface Record {
  id: string;
  amount: number;
  type: 'income' | 'expense';
  category: string;
  note?: string;
  date: string;
}
```

## 关键技术点
- [需要特别注意的技术挑战]
```

---

## 步骤四：AI 代理指令 (AGENTS.md)

**文件**：`AGENTS.md`（或 `.opencode/rules/`）

告诉 AI **项目约束、代码风格、架构规则**，让 AI 生成一致的代码。

### 包含内容

| 类别 | 说明 |
|------|------|
| 项目概述 | 一句话说明项目 |
| 技术栈 | 明确使用的框架/库/版本 |
| 目录结构 | 代码组织方式 |
| 代码规范 | 命名风格、组件模式、Hooks 规则 |
| 架构约束 | 数据流方向、状态管理策略 |
| 限制条件 | 不使用的库、不做的功能 |

### 示例模板

```markdown
# AGENTS.md — AI 代理指令

## 项目概述
[一句话描述]

## 技术栈
- 前端：React 18 + TypeScript + Tailwind CSS
- 构建：Vite
- 状态管理：Zustand
- 数据存储：LocalStorage
- 包管理：pnpm

## 项目结构
```
src/
├── components/    # 可复用 UI 组件
├── pages/         # 页面级组件
├── stores/        # Zustand stores
├── hooks/         # 自定义 Hooks
├── utils/         # 工具函数
└── types/         # TypeScript 类型
```

## 代码规范
- 使用函数组件 + Hooks
- 使用 Tailwind CSS 原子类（禁止其他 CSS 方案）
- 组件文件使用 PascalCase
- 工具函数使用 camelCase
- 所有数据操作通过 Zustand store

## 架构约束
- 数据流：用户操作 → Store → 组件重新渲染
- 禁止直接操作 DOM
- 所有异步操作在 store action 中处理

## 限制条件
- 不引入 UI 组件库（Antd/Element Plus 等）
- 不使用类组件
- 不修改 `index.html`
```

---

## 步骤五：实现与迭代 (Build)

### 正确做法

1. **按 PRD 功能列表逐个实现**
2. 每个功能完成 → 测试 → 提交
3. 使用 AI 对话时明确指示

### 追加需求的处理

| 变更大小 | 处理方式 |
|----------|---------|
| 小改动 | 直接在 AGENTS.md 补充说明 |
| 中等改动 | 更新 PRD + AGENTS.md |
| 大改动 | 重新走完整 5 步流程 |

### 推荐指令模板

```
请按照 AGENTS.md 的规范，实现 [某个功能]。
功能描述：[具体说明]
验收标准：[怎样的结果算完成]
```

---

## 完整项目文件清单

```
project/
├── RESEARCH.md      # 需求研究（步骤一）
├── PRD.md           # 产品需求文档（步骤二）
├── TECH_DESIGN.md   # 技术设计文档（步骤三）
├── AGENTS.md        # AI 代理指令（步骤四）
└── src/             # 代码实现（步骤五）
```

---

## 常见问题

**Q：项目开始后怎么追加需求？**  
A：小改动直接补充 AGENTS.md，中等改动更新 PRD，大改动重新走完整流程。

**Q：AI 生成的代码前后矛盾怎么办？**  
A：检查 AGENTS.md 是否足够详细，确保架构约束写清楚。

**Q：非开发人员能使用这套流程吗？**  
A：可以。步骤一/二/四非开发人员也能完成，步骤三可以借助 AI 生成。
